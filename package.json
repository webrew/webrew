{
  "name": "webrew",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "browser-sync": "browser-sync",
    "polymer": "polymer",
    "firebase": "firebase",
    "sync": "browser-sync start --proxy localhost:8081 --watch --files ./**/*.* --ignore node_modules --logLevel debug",
    "server": "polymer serve"
  },
  "dependencies": {
    "@polymer/lit-element": "^0.6.5"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "firebase-tools": "^6.2.2",
    "polymer-cli": "^1.9.4"
  }
}
